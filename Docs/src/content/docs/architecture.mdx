---
title: Architecture
description: Next.js + Supabase stack and flow
---

import { Badge } from '@astrojs/starlight/components';

Next.js 15 App Router + Supabase (Postgres, Auth, Realtime). Three layers: presentation (pages + components), application (API routes + lib), data (Supabase + external APIs).

## Overview

```mermaid
flowchart TB
    subgraph Client["Client (PWA)"]
        Rider[Rider Dashboard]
        Admin[Admin Dashboard]
    end

    subgraph NextJS["Next.js App Router"]
        Pages["Auth, Dashboard, Admin"]
        API["/api/admin, rider, payments, cron, claims"]
        Lib["adjudicator, fraud, ml, supabase"]
    end

    subgraph Data["Data & Services"]
        Supabase[Supabase]
        ExtAPI[Tomorrow.io, NewsData, OpenRouter, Stripe]
    end

    Client --> NextJS
    NextJS --> Supabase
    NextJS --> ExtAPI
```

**Flow:** Client (PWA) → Next.js (pages, API, lib) → Supabase + external APIs. Cron jobs trigger adjudicator and weekly premium renewal.

---

## Request Flows

### Dashboard Load

```mermaid
flowchart TB
    RQ["1. Request"] --> MW["Middleware\n(session refresh)"]
    MW --> RL["2. Render layout"]
    RL --> FP["Fetch profile"]
    RL --> FPol["Fetch policy"]
    RL --> FC["Fetch claims"]
    RL --> SUB["3. Initialize subscription"]
    SUB --> RT["Supabase Realtime\n(wallet updates)"]
```

### Premium Subscription

```mermaid
flowchart TB
    UI["1. User init\n(rider selects plan + week)"] --> CC["POST /api/payments/create-checkout"]
    CC --> SC["2. Stripe Checkout"]
    SC --> WH["3. Webhook\n/api/payments/webhook"]
    WH --> WP["INSERT weekly_policies"]
    WH --> PT["INSERT payment_transactions"]
    WH --> RD["Redirect back to app"]
```

### Adjudicator (Hourly)

```mermaid
flowchart TB
    Start["runAdjudicator()"] --> Z["1. Discover active zones\nweekly_policies + profiles\ncluster ~11 km grid"]
    Z --> T["2. Check triggers per zone\n(parallel, batch of 5)"]
    T --> Heat["Extreme heat\n(Open-Meteo → Tomorrow.io)"]
    T --> Rain["Heavy rain\n(Tomorrow.io)"]
    T --> AQI["AQI adaptive\n(WAQI → Open-Meteo)"]
    T --> Traffic["Traffic gridlock\n(NewsData + LLM)"]
    T --> Curfew["Curfew / strike\n(NewsData + LLM + geocoding)"]
    T --> DD["Deduplicate\n(skip same trigger type within 30 km)"]

    DD --> Loop["3. For each trigger candidate"]
    Loop --> Evt["INSERT live_disruption_events"]
    Loop --> Pol["Find active policies in geofence"]
    Loop --> Cap["Check weekly claim cap\n(max_claims_per_week)"]
    Loop --> Fraud["runAllFraudChecks()"]
    Loop --> Claims["INSERT parametric_claims\n(status = 'pending_verification')"]

    Claims --> Log["4. Log result to system_logs"]
```


---

## Key Modules

| Module | Role |
|--------|------|
| `lib/adjudicator/run.ts` | Zone discovery, trigger checks, fraud pipeline, claim creation |
| `lib/fraud/detector.ts` | 7 checks: duplicate, rapid claims, weather mismatch, location, device, cluster, baseline |
| `lib/ml/premium-calc.ts` | Base ₹79, max ₹149, +₹8/event, forecast factor |
| `lib/supabase/*` | Browser, server, admin, middleware clients |

---

## Auth

Supabase Auth. Middleware refreshes session. Route groups: `(auth)` public, `(dashboard)` rider, `(admin)` admin-only (email in `ADMIN_EMAILS` or `role=admin`).

---

## Realtime

`RealtimeWallet` subscribes to `parametric_claims` by `policy_id`. New claim → balance updates without reload.
