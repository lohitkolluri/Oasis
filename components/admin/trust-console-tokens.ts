/** Shared scroll heights for parametric trust tables (embedded in tabs). */
export const TRUST_TABLE_SCROLL_CLASS = 'h-[min(380px,52vh)] w-full min-h-0';
/** Wider min width so ledger columns (Time … Evidence) don’t crush on narrow tabs. */
export const TRUST_TABLE_INNER_MIN_WIDTH = 'min-w-[820px]';
